{
  "name": "mairie-platform",
  "version": "1.0.0",
  "description": "Plateforme complète de gestion pour mairies",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix landing\" \"npm run dev --prefix onboarding-api\" \"npm run dev --prefix mairie-instance/backend\" \"npm run dev --prefix mairie-instance/frontend\" \"npm run dev --prefix mairie-instance/backoffice\"",
    "build": "npm run build --prefix landing && npm run build --prefix mairie-instance/frontend && npm run build --prefix mairie-instance/backoffice",
    "setup": "bash setup.sh",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "docker:logs": "docker-compose logs -f",
    "create:mairie": "bash deploy-scripts/create-mairie.sh",
    "backup": "bash deploy-scripts/backup-mairie.sh",
    "update": "bash deploy-scripts/update-mairie.sh"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
